import { notFound } from "next/navigation";
import type { Metadata } from "next";
import { ButtonLink } from "@/components/ButtonLink";
import { HomeCard, StatusBadge, homeBadges } from "@/components/HomeCard";
import { LeadForm } from "@/components/LeadForm";
import { MediaPlaceholder } from "@/components/MediaPlaceholder";
import { homes, getHomeById } from "@/data/homes";
import { siteInfo } from "@/data/site";

export function generateStaticParams() { return homes.map((home) => ({ slug: home.id })); }
export function generateMetadata({ params }: { params: { slug: string } }): Metadata { const home = getHomeById(params.slug); return { title: home?.seoTitle ?? (home ? `${home.name} | Easy HomeSource` : "Home Details | Easy HomeSource"), description: home?.seoDescription ?? home?.description ?? "View manufactured home details from Easy HomeSource in Brooksville, Florida." }; }

export default function HomeDetailPage({ params }: { params: { slug: string } }) {
  const home = getHomeById(params.slug); if (!home) notFound();
  const related = homes.filter((item) => item.id !== home.id && !item.specialOffer).slice(0, 3);
  const specs = [
    home.beds != null && { label: "Beds", value: home.beds }, home.baths != null && { label: "Baths", value: home.baths }, home.squareFeet != null && { label: "Sq. Ft.", value: home.squareFeet.toLocaleString() }, home.size && { label: "Size", value: home.size }
  ].filter(Boolean) as { label: string; value: string | number }[];
  const specsSummary = specs.map((s) => `${s.value} ${s.label.toLowerCase()}`).join(" • ");
  return <main className="px-4 py-12"><div className="mx-auto grid max-w-6xl gap-10 lg:grid-cols-[1fr_420px]"><section><div className="flex flex-wrap items-center gap-3">{homeBadges(home).map((badge) => <span key={badge} className="rounded-full bg-ehsBlue px-3 py-1 text-xs font-black text-white">{badge}</span>)}<StatusBadge status={home.status} /></div>{home.manufacturer && <p className="mt-6 font-black text-ehsBlue">{home.manufacturer}</p>}<h1 className="mt-2 text-4xl font-black text-ehsBlack sm:text-5xl">{home.name}</h1><p className="mt-4 max-w-3xl text-lg leading-8 text-ehsBlack/75">{home.longDescription ?? home.description}</p><div className="mt-8 grid gap-4 sm:grid-cols-[1.5fr_1fr]"><div className="rounded-[2rem] bg-gradient-to-br from-ehsBlack via-ehsBlack to-ehsBlue p-6 text-white shadow-xl shadow-ehsBlack/10"><p className="text-sm font-black uppercase tracking-wide text-white/70">Photo gallery</p><div className="mt-28 rounded-3xl bg-white/10 p-5 backdrop-blur"><p className="text-3xl font-black">{home.name}</p>{specsSummary && <p className="mt-2 text-white/75">{specsSummary}</p>}</div></div><div className="grid gap-4"><MediaPlaceholder /><MediaPlaceholder /></div></div><dl className="mt-8 grid grid-cols-2 gap-4 sm:grid-cols-5"><Detail label="Price" value={home.price ?? "Call for current pricing"} />{specs.map((spec) => <Detail key={spec.label} {...spec} />)}</dl><div className="mt-8 grid gap-6 lg:grid-cols-2"><div className="rounded-3xl border border-borderGray bg-white p-6"><h2 className="text-2xl font-black text-ehsBlack">Key features</h2><ul className="mt-4 grid gap-3">{home.features.map((feature) => <li key={feature} className="font-semibold text-ehsBlack/75">✓ {feature}</li>)}</ul></div><MediaPlaceholder title="Floor plan coming soon" /><MediaPlaceholder title="Video coming soon" /><div className="rounded-3xl border border-borderGray bg-ehsSoftBlue p-6"><h2 className="text-2xl font-black text-ehsBlack">Next steps</h2><p className="mt-4 leading-8 text-ehsBlack/75">Request current pricing, availability, financing options, delivery, setup, permitting, and tour details.</p></div></div><div className="mt-8 rounded-[2rem] bg-ehsBlue p-8 text-white"><h2 className="text-3xl font-black">Want pricing for this home?</h2><p className="mt-3 leading-7 text-white/75">Ask about current availability, financing guidance, delivery to your Florida site, setup, permitting, and move-in timing.</p><div className="mt-6 flex flex-col gap-3 sm:flex-row"><ButtonLink href="#lead-form">Get Pricing</ButtonLink><ButtonLink href="/contact" variant="secondary">Schedule a Tour</ButtonLink></div></div><p className="mt-6 rounded-2xl border border-borderGray bg-white p-4 text-sm font-semibold leading-6 text-ehsBlack/70">{siteInfo.pricingDisclaimer}</p><section className="mt-12"><h2 className="text-3xl font-black text-ehsBlack">Related homes</h2><div className="mt-6 grid gap-6 lg:grid-cols-3">{related.map((item) => <HomeCard key={item.id} home={item} />)}</div></section></section><aside id="lead-form" className="lg:sticky lg:top-24 lg:self-start"><LeadForm interestedHome={home.name} interestedHomeSlug={home.id} cta="Get Pricing" sourcePage={`${home.name} home detail`} /></aside></div></main>;
}
function Detail({ label, value }: { label: string; value: string | number }) { return <div className="rounded-2xl bg-ehsSoftBlue p-4"><dt className="text-xs font-black uppercase tracking-wide text-ehsBlack/60">{label}</dt><dd className="mt-1 text-xl font-black text-ehsBlack">{value}</dd></div>; }
