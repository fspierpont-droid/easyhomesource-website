import { HomeCard } from "@/components/HomeCard";
import { getFeaturedHomes } from "@/data/homes";
export const metadata = { title: "Featured Homes", description: "Browse featured manufactured homes from Easy HomeSource in Brooksville, Florida." };
export default function FeaturedHomesPage(){ const featured=getFeaturedHomes(); return <main className="px-4 py-12"><div className="mx-auto max-w-6xl"><p className="font-black text-ehsBlue">Featured Homes</p><h1 className="mt-2 text-4xl font-black text-ehsBlack">Featured manufactured homes</h1><p className="mt-4 max-w-3xl text-lg leading-8 text-ehsBlack/75">Compare selected homes and request current pricing, delivery, setup, and financing-option details.</p><div className="mt-8 grid gap-6 lg:grid-cols-3">{featured.map((home)=><HomeCard key={home.id} home={home}/>)}</div></div></main> }
