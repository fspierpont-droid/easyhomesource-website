import { HomeCard } from "@/components/HomeCard";
import { homes } from "@/data/homes";
export const metadata = { title: "Special Offers", description: "See current special-offer manufactured homes and request details from Easy HomeSource." };
export default function SpecialOffersPage(){ const offers=homes.filter(h=>h.isSpecialOffer); return <main className="px-4 py-12"><div className="mx-auto max-w-6xl"><p className="font-black text-ehsBlue">Special Offers</p><h1 className="mt-2 text-4xl font-black text-ehsBlack">Manufactured home offers</h1><p className="mt-4 max-w-3xl text-lg leading-8 text-ehsBlack/75">Offers, availability, pricing, financing, delivery, setup, taxes, fees, permits, site conditions, lender approval, and final costs are subject to change and final quote.</p><div className="mt-8 grid gap-6 lg:grid-cols-3">{offers.map((home)=><HomeCard key={home.id} home={home}/>)}</div></div></main> }
