javascript:(()=>{ "use strict"; const MEDIA_EXT = /\.(?:jpe?g|png|webp|avif|gif|pdf|mp4|mov|m4v|webm)(?:[?#].*)?$/i; const VIDEO_HOST = /(?:youtube\.com|youtu\.be|vimeo\.com|matterport\.com)/i; const BAD_WORDS = /(?:favicon|apple-touch-icon|logo|sprite|social|facebook|instagram|twitter|x-icon|linkedin|pinterest|pixel|tracking|analytics|avatar|badge|loader|spinner)/i; const SUSPICIOUS_QUERY = /(?:token|auth|session|cookie|signature|signed|jwt|bearer|access[_-]?key|secret|credential|password|expires|policy|x-amz|x-goog|cf-signature)/i; const media = []; const seen = new Set(); const absolute = (value) => { try { return new URL(value, location.href).toString(); } catch { return ""; } }; const stripUnsafeQuery = (url) => { try { const parsed = new URL(url); for (const key of Array.from(parsed.searchParams.keys())) { if (SUSPICIOUS_QUERY.test(key) || SUSPICIOUS_QUERY.test(parsed.searchParams.get(key) || "")) parsed.searchParams.delete(key); } parsed.hash = ""; return parsed.toString(); } catch { return url; } }; const urlsFromSrcset = (value) => (value || "").split(",").map((part) => part.trim().split(/\s+/)[0]).filter(Boolean); const cssUrls = (value) => Array.from((value || "").matchAll(/url\(["']?([^"')]+)["']?\)/gi)).map((m) => m[1]); const isVisible = (el) => { const rect = el.getBoundingClientRect(); const style = getComputedStyle(el); return rect.width > 8 && rect.height > 8 && style.display !== "none" && style.visibility !== "hidden" && Number(style.opacity || 1) > 0; }; const classify = (text, type) => { const t = `${text || ""}`.toLowerCase(); if (type === "brochure" || /brochure|\.pdf/.test(t)) return "brochure"; if (type === "video" || /video|youtube|vimeo|matterport|tour|\.mp4|\.mov|\.webm/.test(t)) return "video"; if (/floor\s?plan|layout|blueprint/.test(t)) return "floorplan"; if (/kitchen/.test(t)) return "kitchen"; if (/bath|shower|toilet|vanity/.test(t)) return "bathroom"; if (/bedroom|bed\b|master|primary/.test(t)) return "bedroom"; if (/exterior|front|porch|elevation|hero|outside/.test(t)) return "exterior"; if (/interior|living|dining|utility|laundry|room/.test(t)) return "interior"; return "other"; }; const typeFor = (url, hint) => { if (/\.pdf(?:[?#]|$)/i.test(url) || /brochure/i.test(hint || "")) return "brochure"; if (/\.(?:mp4|mov|m4v|webm)(?:[?#]|$)/i.test(url) || VIDEO_HOST.test(url)) return "video"; if (/\.(?:jpe?g|png|webp|avif|gif)(?:[?#]|$)/i.test(url)) return "image"; return "media"; }; const shouldKeep = (url, meta = {}) => { if (!url || /^data:|^blob:|^javascript:/i.test(url)) return false; if (BAD_WORDS.test(url) || BAD_WORDS.test(meta.alt || "")) return false; if (!MEDIA_EXT.test(url) && !VIDEO_HOST.test(url) && !/image|video|pdf|brochure|floorplan/i.test(meta.hint || "")) return false; if ((meta.width && meta.width < 180) || (meta.height && meta.height < 120)) return false; return true; }; const add = (rawUrl, meta = {}) => { const absoluteUrl = absolute(rawUrl); if (!absoluteUrl) return; const url = stripUnsafeQuery(absoluteUrl); if (!shouldKeep(url, meta) || seen.has(url)) return; seen.add(url); const type = meta.type || typeFor(url, meta.hint); const text = [url, meta.alt, meta.hint, document.title, location.pathname].filter(Boolean).join(" "); media.push({ url, type, categoryGuess: classify(text, type), alt: meta.alt || "", width: meta.width || undefined, height: meta.height || undefined }); }; document.querySelectorAll("img").forEach((img) => { const meta = { alt: img.alt || img.getAttribute("aria-label") || "", width: img.naturalWidth || img.width, height: img.naturalHeight || img.height, hint: img.outerHTML.slice(0, 500) }; [img.currentSrc, img.src, img.getAttribute("data-src"), ...urlsFromSrcset(img.srcset), ...urlsFromSrcset(img.getAttribute("data-srcset"))].forEach((url) => add(url, meta)); }); document.querySelectorAll("picture source, source[srcset]").forEach((source) => urlsFromSrcset(source.srcset || source.getAttribute("data-srcset")).forEach((url) => add(url, { hint: source.outerHTML.slice(0, 300) }))); document.querySelectorAll("a[href]").forEach((a) => { if (/\.pdf(?:[?#]|$)|brochure/i.test(a.href + a.textContent)) add(a.href, { type: "brochure", alt: a.textContent.trim(), hint: a.outerHTML.slice(0, 300) }); }); document.querySelectorAll("iframe[src], video[src], video source[src]").forEach((el) => add(el.src, { type: "video", hint: el.outerHTML.slice(0, 300), width: el.clientWidth, height: el.clientHeight })); document.querySelectorAll("body *").forEach((el) => { if (isVisible(el)) cssUrls(getComputedStyle(el).backgroundImage).forEach((url) => add(url, { width: el.clientWidth, height: el.clientHeight, hint: el.className || el.id || "background-image" })); }); if (performance && performance.getEntriesByType) performance.getEntriesByType("resource").forEach((entry) => add(entry.name, { hint: entry.initiatorType || "performance" })); const output = { sourcePage: location.href, pageTitle: document.title, capturedAt: new Date().toISOString(), media: media.sort((a, b) => (a.categoryGuess === "other") - (b.categoryGuess === "other")) }; const json = JSON.stringify(output, null, 2); const html = `<!doctype html><title>EHS Media Capture JSON</title><style>body{font-family:system-ui;margin:24px}textarea{width:100%;height:75vh;font-family:ui-monospace,monospace}</style><h1>EHS Media Capture JSON</h1><p>Copy this JSON and save it in <code>imports/browser-captures/[home-slug].json</code>.</p><textarea autofocus>${json.replace(/[&<>]/g, (c) => ({"&":"&amp;","<":"&lt;",">":"&gt;"}[c]))}</textarea>`; navigator.clipboard?.writeText(json).catch(() => {}); const win = window.open("", "_blank"); if (win) { win.document.open(); win.document.write(html); win.document.close(); } else { alert(json); } })();
