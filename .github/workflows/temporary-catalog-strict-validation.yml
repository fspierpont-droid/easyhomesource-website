name: Temporary catalog behavior validation

on:
  push:
    branches:
      - 'fix/catalog-seed-duplicate-fields'
    paths:
      - '.github/workflows/temporary-catalog-strict-validation.yml'

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout repair branch
        uses: actions/checkout@v4
        with:
          ref: fix/catalog-seed-duplicate-fields
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: npm
      - name: Install locked dependencies
        run: npm ci
      - name: ESLint
        run: npm run lint
      - name: Portal and GHL tests
        run: npm run test:ghl
      - name: Production Next build
        run: npm run build
